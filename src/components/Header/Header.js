import React from 'react';

import { Link } from 'gatsby';

const Header = () => {
  return (
    <header className='Header'>
      <Link to='/'>Home</Link>
    </header>
  );
};

export default Header;
